Django>=5.1,<5.3
djangorestframework>=3.15
django-cors-headers>=4.4
PyJWT>=2.9
python-dotenv>=1.0
razorpay>=1.4
argon2-cffi>=23.1

# Pure-Python MySQL driver: installs anywhere, no libmysqlclient headers needed.
# `mysqlclient` is faster if you can build it — swap it in and drop the shim in
# config/__init__.py.
PyMySQL>=1.1
